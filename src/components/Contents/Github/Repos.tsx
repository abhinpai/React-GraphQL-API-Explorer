import React, { PureComponent } from "react";
import { ComponentHeader } from "../../Header/Header";

export default class Repo extends PureComponent {
  render() {
    return (
      <div>
        <ComponentHeader title={"Github Repo"} />
      </div>
    );
  }
}

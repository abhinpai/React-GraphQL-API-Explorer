import React, { PureComponent } from "react";
import { ComponentHeader } from "../../Header/Header";

export default class TwitterUser extends PureComponent {
  render() {
    return (
      <div>
        <ComponentHeader title={"TwitterUser"} />
      </div>
    );
  }
}
